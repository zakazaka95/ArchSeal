# ArchSeal V2 evidence demo

This document is a small, reviewable change used to demonstrate ArchSeal V2 against this repository's accepted architecture policy.

The review must read `.archseal/policy.json` from the pull request's pinned base commit, use the ADR path declared by that policy, verify that all required evidence is complete, and bind its final verdict to the exact base commit, head commit, policy hash, and transaction-context review ID.

No application behavior, contract state model, wallet flow, or source-of-truth rule is changed by this documentation-only pull request.
